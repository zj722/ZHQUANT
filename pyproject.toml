[project]
name = "zhquant"
version = "0.1.0"
description = "Minimal strategy DSL validation pipeline"
requires-python = ">=3.10"
dependencies = ["pandas>=2.0", "yfinance>=0.2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
